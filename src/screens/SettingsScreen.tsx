import React from 'react';
import { PlaceholderScreen } from './PlaceholderScreen';

/** TODO(day8): settings + paywall, AI engine picker, subscription/cosmetics. */
export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      subtitle="Preferences, AI engine, and the paywall will live here."
      comingOn="Coming Day 8"
    />
  );
}
