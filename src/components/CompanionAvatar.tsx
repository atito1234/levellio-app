/**
 * Backwards-compatible alias: the Wisp companion is now a dedicated SVG
 * component. Existing call sites import `CompanionAvatar` with `{ stage, size }`.
 */
export { Wisp as CompanionAvatar } from './Wisp';
